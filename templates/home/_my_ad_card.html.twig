
<div class="mb-1">
    <div class="row">
        <div class="col-4 px-0 pe-1">
            <img src="https://via.placeholder.com/400x300" class="img-fluid" alt="...">
        </div>
        <div class="col-8 px-0">
            <h6>{{ad.title|u.truncate(30,'...')}}</h6>
            <div>{{ad.price}}</div> 
            <div>
                <i class="fa-regular fa-eye color-primary">{{ad.viewsCount}}</i>
                <i class="fa-regular fa-heart color-danger">{{ad.favorites|length}}</i>
                <i class="fa-solid fa-phone color-primary">{{ad.messageContactCount}}</i>
                <i class="fa-regular fa-whatsapp color-success">{{ad.whatsappContactCount}}</i>
            </div>
        </div>
    </div>
</div>